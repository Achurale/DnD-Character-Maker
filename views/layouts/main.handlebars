<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/style.css">

  <title>Home Page - Dungeons & Dragons Character Generator</title>
</head>

<body class="bg-secondary">
  <div class="flex-column justify-space-around">
    <header class="display-flex justify-space-between align-center p-2 border-bottom border-danger border-2">
      <h1 class="text-center">
        <div>
          <a href="/">Dungeons & Dragons Character Maker</a>
        </div>
      </h1>
      <div class="text-end">
        <nav>
          <!-- Conditionally render login or logout links -->
          {{#if logged_in}}
          {{!-- <a href="/profile">profile</a> | --}}
          <button class="no-button" id="logout">Logout</button>
          {{else}}
            <a href="/login.handlebars" id="login">Login</a>
          {{/if}}
        </nav>
      </div>
    </header>

    <main class="container container-fluid mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>

    <footer class="text-center border-top border-danger border-2">
      <div>
        <h3>Contact Us!</h3>
      </div>
      <div>
        <a href="https://github.com/aridel96">Arianna</a>
        -
        <a href="https://github.com/BreePrimmer">Bree</a>          {{!-- We can add out Github OR linkedIn accounts OR email here --}}
        -
        <a href="https://github.com/Achurale">Chuyi</a>
      </div>
    </footer>
  </div>
  
  <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
    <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
